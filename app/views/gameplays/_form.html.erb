<%= form_with(model: gameplay) do |form| %>
  <% if gameplay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gameplay.errors.count, "error") %> prohibited this gameplay from being saved:</h2>

      <ul>
        <% gameplay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
